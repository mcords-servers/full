#include "kit.h"

int engine_lobby_process_player(int fd) {
    int r = 0;
    int engine_lobby_process_player_chunks(int fd);
    r += engine_lobby_process_player_chunks(fd);
    // r *= 10;


    return r;
}